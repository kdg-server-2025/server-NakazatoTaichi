## 3回目の授業
```bash
# brewでhadolintをインストール
brew install hadolint

# hadolintのバージョン確認
hadolint --version

# hadolint実行コマンド
hadolint Dockerfile

# 今回のエラーwarning内容
Dockerfile:3 DL3045 warning: `COPY` to a relative destination without `WORKDIR` set.

# go-hello-02 という名前でイメージをビルド
docker run go-hello-02

# イメージを実行
docker run go-hello-02

```